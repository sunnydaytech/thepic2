var auth = require('../../lib/recent');
var passport = require('koa-passport');
var assert = require('assert');

describe('recent', function() {
  it('Should record recent uploads.', function() {
  });
});

